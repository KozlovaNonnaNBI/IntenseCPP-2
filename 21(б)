#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float a, b;
    cout << "Введите a" << endl;
    cin >> a;
    cout << "Введите b" << endl;
    cin >> b;
    float x = 3.56*pow((a + b),3) - 5.8*pow(b,2) + 3.8*a - 1.5;
    cout << x << endl;
    return 0;
}
