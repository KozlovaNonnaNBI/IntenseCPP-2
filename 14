#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float a;
    cout << "Введите a" << endl;
    cin >> a;
    float otvet = (pow(a,2) + 1)/sqrt(pow(a,2) + 1);
    cout << otvet << endl;
    return 0;
}
